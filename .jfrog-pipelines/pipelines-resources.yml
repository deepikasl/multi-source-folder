resources:
  - name: myRepo_deepika
    type: GitRepo
    configuration:
      gitProvider: jfrog_github
      path: deepikasl/multi-source-folder
      branches:
        include: {{ gitBranch }}
      buildOn:
        commit: true
  
