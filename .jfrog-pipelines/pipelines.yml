resources:
  - name: testInfo
    type: BuildInfo
    configuration:
      sourceArtifactory: art
      buildName: 'name'
      buildNumber: '1'

pipelines:
  - name: pipeline
    configuration:
      nodePool: k8s
    steps:
      - name: mystep
        type: Bash
        configuration:
          inputResources:
            - name: testInfo
        execution:
          onExecute:
            - echo "executing..."
