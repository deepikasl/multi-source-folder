pipelines:
  - name: sub_pipeline_poc
    steps:
      - name: first_step
        type: Bash
        execution:
          onExecute:
            - echo "executing first step"
      - name: second_step
        type: Bash
        configuration:
          inputSteps:
            - name: first_step
        execution:
          onExecute:
            - echo "executing second step"

            
